import { Bar, BarChart, CartesianGrid, Legend, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
type Props={dailyData:Array<{date:string;studyMinutes:number;jobMinutes:number;gymMinutes:number}>;studyBySubject:Array<{subject:string;minutes:number}>;preferences:{student:boolean;gym:boolean;partTimeJob:boolean}};
const axisTick={fill:"var(--text-muted)",fontSize:11};const tooltipStyle={background:"var(--surface-elevated)",border:"1px solid var(--border)",borderRadius:"var(--radius-sm)",color:"var(--text-primary)",fontSize:12};
export function AnalyticsCharts({dailyData,studyBySubject,preferences}:Props){const hasTrackedTime=dailyData.some((row)=>row.studyMinutes>0||row.jobMinutes>0||row.gymMinutes>0);const chartData=dailyData.map((row)=>({date:new Intl.DateTimeFormat("en",{month:"short",day:"numeric"}).format(new Date(`${row.date}T12:00:00`)),Study:row.studyMinutes/60,"Part-Time Job":row.jobMinutes/60,Workouts:row.gymMinutes/60}));return <section className="chart-grid"><article className="chart-card chart-wide"><div className="chart-heading"><h2>Tracked hours</h2><span>Actual time by day</span></div><div className="chart-frame">{hasTrackedTime?<ResponsiveContainer width="100%" height="100%"><LineChart data={chartData} margin={{top:8,right:8,left:-12,bottom:0}}><CartesianGrid vertical={false} stroke="var(--chart-grid)"/><XAxis dataKey="date" tick={axisTick} tickLine={false}/><YAxis unit="h" tick={axisTick} tickLine={false}/><Tooltip contentStyle={tooltipStyle}/><Legend/>{preferences.student&&<Line type="monotone" dataKey="Study" stroke="var(--study)" strokeWidth={2} dot={false}/>} {preferences.partTimeJob&&<Line type="monotone" dataKey="Part-Time Job" stroke="var(--job)" strokeWidth={2} dot={false}/>} {preferences.gym&&<Line type="monotone" dataKey="Workouts" stroke="var(--gym)" strokeWidth={2} dot={false}/>}</LineChart></ResponsiveContainer>:<p className="chart-empty">No completed activity in this range.</p>}</div></article>{preferences.student&&<article className="chart-card"><div className="chart-heading"><h2>Study by subject</h2><span>Actual focused hours</span></div><div className="chart-frame">{studyBySubject.length?<ResponsiveContainer width="100%" height="100%"><BarChart data={studyBySubject.map((row)=>({...row,Hours:row.minutes/60}))} layout="vertical"><CartesianGrid vertical={false} stroke="var(--chart-grid)"/><XAxis type="number" unit="h" tick={axisTick}/><YAxis dataKey="subject" type="category" width={92} tick={axisTick}/><Tooltip contentStyle={tooltipStyle}/><Bar dataKey="Hours" fill="var(--study)" radius={[0,3,3,0]}/></BarChart></ResponsiveContainer>:<p className="chart-empty">No subject study time in this range.</p>}</div></article>}</section>}
